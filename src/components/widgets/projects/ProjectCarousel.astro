---
import SimpleCarousel from '@atoms/SimpleCarousel';
import type { CarouselProps } from '@atoms/SimpleCarousel';

const { images, ...props } = Astro.props as CarouselProps;
---

<figure {...props}>
	<SimpleCarousel images={images} client:load />
</figure>
